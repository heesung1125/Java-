/*
  	Instauce를 생성했다.
	→ 객체가 사용 할 메모리 공간을 할당 했다.
	
	# Java의 RegEx(정규화 표현식)
	[] → 1글자를 의미한다.
		ex) String[] a val.split("[a,b]"); val의 값을 a,b를 기준으로 문자열을 잘라서 배열 a 변수에 입력 하라.
		
	★ 정규화 표현식 주의사항
		예약어 : 목적이 정해져 있는 키워드  ─┬─ 변수명
								   	  ├─ 메소드명
									  └─ 정규식
									  
	int num1 = 10;		메모리에 저장 될때는 2진수로 저장된다. 1010(2진수는 길어서 보기 어려우니 16진수로 A)
	String num2 = "10"; 메모리에 저장 될때는 Unicode로 저장된다. '1'의 ASCII코드 16진수는 31 메모리 저장은 0011 0001로 들어간다.
														   '0'의 ASCII코드 16진수는 30 메모리 저장은 0011 0000으로 들어간다.
			* ASCII코드를 유니코드로 바꿀때는 앞에 00을 붙여 준다. 그러므로 저장되는 메모리는...
			* 	1 = 0000 0000 0011 0001 (16진수 : 0031)
			*   0 = 0000 0000 0011 0000 (16진수 : 0030)

	class 밑에 선언된 변수 : 멤버변수 = 필드(Field)
	Method,반복문,비교문 밑에 선언된 변수 : 지역변수

*/	
		
/* 2016-11-18
* 버전관리도구
* 
* CVCS(중앙집중식 버전 관리 시스템)
* CVS
* SVN
* - 단점
* 	중앙 서버가 잘못되면 모든 것이 잘못된다
* 
* DVCS(분산 버전 관리 시스템)
* git
* Source tree
* 
* 
* local Repository(로컬 저장소)
* 
* 
* 
* 
1. commit할 파일&디렉토리가 있는 곳 으로 이동

: cd [디렉토리명]

2. git 등록 초기화

: git init

→ 지금 있는 디렉토리에 숨김폴더로 .git가 생성된다.
	※ 확인 방법 dir/a 입력 하면 숨김 폴더가 보인다.

3. 등록할 이메일과 이름을 입력한다.(한번만 등록 하면 됨)

: git config --global user.email "you@example.com"
: git config --global user.name "Your Name"

4. 제대로 입력 됬는지 확인

: git config --list

5. commit할 파일 및 디렉토리 설정

: git add * or git add .
	→ 해당 디렉토리에 있는 전체를 commit할때 입력

: git add [파일명] [파일명] ... [디렉토리명]
	→ 해당 디렉토리에 있는 일부를 commit할때 입력

6. commit 실행

: git commit -m "주석문"
	→ -m : commit와 동시에  주석문을 입력한다.

7. EGit 파일 가져오기
http://huskdoll.tistory.com/471

8. eclipse EGit 시나리오별 사용법 
http://lng1982.tistory.com/177 
*/

/* 2016-11-21
Java Appication의 메모리 구조 & 객체 생성 과정

↓ test.class ↓

public class test{
	int age;			← 맴버 변수 = 필드 이며, 인스턴스 변수
	String name ;		← 맴버 변수 = 필드 이며, 인스턴스 변수
	static int salary;	← 맴버 변수 = 필드 이며, 클래스 변수 (static 가 붙으면...)
 public static test(){ ← 생성자
  [초기화 코드]
 }
 
 public void setAge(int age){ setter 설정
 	this.age = age();
 }
 public int getAge(){ getter 설정
 	return this.age;
 }
 
 public void method1(){
 	System.out.println("test")
 }
}

↓ start.class ↓ ← 처음 실행되는 class

public class start{
	public static void main(String[] args) {					    ____객체생성____
		test t1 = new test(); ← 객체 생성 (클래스명_객체로가기위한주소변수 = new_생성자명();)
		t1.method1(); ← 해당 객체에 있는 메소드 불러오기
		t1.age = 2; ← test 클래스의 age 변수에 2값을 저장
		t1.setAge(2); ← 위와 같은 의미 단, setAge 메소드가 해당 클레스내에 설정 해 주어야 한다.(상단 test.class 참조) 이게 setter
		System.out.pnistln(t1.age); ← test 클래스의 age 변수의 값을 출력
		System.out.pnistln(t1.getAge); ← 위와 같은 의미 단, getAge 메소드가 해당 클레스내에 설정 해 주어야 한다.(상단 test.class 참조) 이게 getter
	}
}



기본 자료형(Primitive Data Type)
int, char, short, long, double, float

참조 자료형(Reference Data Type) Data를 대량으로 다룰 때
 Array Type(배열)
 	String
 	StringBuilder
 
 Enumeration(열거)

----------------------------------------------------------------
Multi Thread
 * 한개의 Application에서 여러개의 Thread를 작동

Stack : Data관리방식 // 입력 순서대로 자리 잡고 처리는 역순으로 처리한다.(FILO)First In Last Out 
  ↕
(First In First Out) // 입력 순서대로 자리 잡고 처리도 정순으로 처리 한다.


 * 참조(Reference) 자료형 : 실제 값이 들어있지 않고 값을 찾아 갈 수 있는 주소값을 적어주는 자료형.

	// 객체(object) ≒  인스턴스(Instance)
		└객체 가 좀 더 넓은 범위 인스턴스는 그 안에 포함 쯤??
 * getter / setter
	getter = get + er : 인스턴스 변수의 값 얻어오기
	setter = set + er : 인스턴스에 값 설정하기
	
 * static 키워드


 */